using UnityEngine;
using System.Collections;

public class Selector : MonoBehaviour {	//many game items need an index and a isSelected flag
	
	public bool isSelected = true;
	public int 
		index = -1,
		iRow = 0,
		jCol = 0;

}
